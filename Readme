The Football Tracker is a basic CRUD application that allows the user to keep up to date with their favourite teams. The User can Add,Update and Delete fixture's.
In the Python module during CodeClan, I created a Football tracker which was referred to when using the fyne tools.